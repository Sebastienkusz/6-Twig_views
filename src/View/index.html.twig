{% extends 'base.html.twig' %}

{% block title %}
    Twig views
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            {% for product in products %}
                <div class="col-4">{{ product }}</div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
